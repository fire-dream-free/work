我来为这个打飞机游戏项目创建一个详细的 README 文件。

```markdown
# Python 打飞机游戏 🎮

一个使用 Pygame 库开发的经典射击游戏，玩家控制飞机躲避并击落敌机，挑战最高分数！

## 📋 项目简介

这是一个经典的纵向卷轴射击游戏（STG），玩家操控一架青色方块飞机，自动发射子弹击落不断下落的红色敌机。随着分数提高，游戏难度会逐渐增加，包括更多弹道、更快的敌机速度和更多的敌机生成数量。

## ✨ 游戏特性

- **自动射击系统**：飞机自动发射子弹，无需手动按键
- **多弹道升级**：每获得 500 分，子弹弹道数量增加 1 个（最多可扩展）
- **难度递增**：
  - 每 500 分敌机速度提升 50%
  - 每 500 分每次生成的敌机数量增加 2 个
- **实时数据显示**：显示当前分数、弹道数量、敌机速度倍数、敌机生成数量
- **简洁的视觉效果**：使用几何图形表示游戏元素
  - 🔵 青色方块：玩家飞机
  - 🟡 黄色矩形：子弹
  - 🔴 红色方块：敌机

## 🛠️ 环境要求

- Python 3.6 或更高版本
- Pygame 库

## 📦 安装步骤

### 1. 安装 Python

确保你的计算机已安装 Python 3.6+。可以从 [Python 官网](https://www.python.org/downloads/) 下载。

### 2. 安装 Pygame

在终端或命令提示符中运行：

```
bash
pip install pygame
```
等待出现以下信息即表示安装成功：

```

Successfully installed pygame
```
### 3. 运行游戏

在项目目录下运行：

```
bash
python 计算机作业.py
```
或者：

```
bash
py 计算机作业.py
```
## 🎮 游戏操作

### 控制方式

| 按键 | 操作 |
|------|------|
| ⬅️ 左箭头键 (←) | 向左移动 |
| ➡️ 右箭头键 (→) | 向右移动 |
| ⬆️ 上箭头键 (↑) | 向上移动 |
| ⬇️ 下箭头键 (↓) | 向下移动 |
| 任意键（游戏结束后） | 重新开始游戏 |
| 关闭窗口 | 退出游戏 |

### 游戏规则

1. **目标**：尽可能击落更多敌机，获得高分
2. **得分**：每击落一架敌机获得 **20 分**
3. **失败条件**：玩家飞机与敌机碰撞则游戏结束
4. **重玩**：游戏结束后按任意键即可重新开始

## 📊 游戏机制详解

### 难度升级系统

游戏采用基于分数的动态难度调整机制：

#### 弹道数量
- **计算公式**：`弹道数 = 1 + (分数 // 500)`
- **初始状态**：1 个弹道（中央发射）
- **500 分时**：2 个弹道（左右分布）
- **1000 分时**：3 个弹道（左中右分布）
- **1500 分以上**：4 个及以上弹道（均匀分布）

#### 敌机速度
- **计算公式**：`速度倍数 = 1.0 + (分数 // 500) × 0.5`
- **初始状态**：1.0x（基础速度 2-4 随机）
- **500 分时**：1.5x
- **1000 分时**：2.0x
- **1500 分时**：2.5x

#### 敌机生成数量
- **计算公式**：`生成数量 = 1 + (分数 // 500) × 2`
- **初始状态**：每次生成 1 架敌机
- **500 分时**：每次生成 3 架敌机
- **1000 分时**：每次生成 5 架敌机
- **1500 分时**：每次生成 7 架敌机

### 游戏循环逻辑

1. **帧率控制**：锁定 60 FPS，确保流畅体验
2. **自动射击**：每 250 毫秒发射一次子弹
3. **敌机生成**：每 800 毫秒生成一批敌机
4. **碰撞检测**：
   - 子弹击中敌机 → 两者消失，分数 +20
   - 玩家撞到敌机 → 游戏结束

## 💻 代码结构

### 主要类

#### `Player`（玩家飞机）
- **属性**：位置坐标 (x, y)、尺寸 (w, h)、移动速度
- **方法**：
  - `move(dx, dy)`：处理移动并限制边界
  - `draw()`：绘制青色矩形飞机

#### `Bullet`（子弹）
- **属性**：位置坐标、尺寸、向上飞行速度
- **方法**：
  - `update()`：更新位置（向上移动）
  - `draw()`：绘制黄色矩形子弹

#### `Enemy`（敌机）
- **属性**：位置坐标、尺寸、随机速度（可受倍数影响）
- **方法**：
  - `update()`：更新位置（向下移动）
  - `draw()`：绘制红色矩形敌机

### 核心函数

#### `collide(a, b)`
实现 AABB（轴对齐边界框）碰撞检测算法，判断两个矩形是否相交。

#### `main()`
游戏主循环，包含：
- 事件处理（输入、退出）
- 游戏状态更新
- 碰撞检测
- 画面渲染
- UI 信息显示

## 🎯 游戏技巧

1. **初期策略**（0-500 分）：
   - 熟悉移动操作
   - 尽量保持在屏幕下方，便于观察和躲避

2. **中期策略**（500-1500 分）：
   - 利用多弹道优势，覆盖更宽的攻击范围
   - 注意敌机速度加快，提前规划移动路线

3. **后期策略**（1500 分以上）：
   - 敌机密集且快速，优先保证生存
   - 在屏幕底部横向移动，避免被包围

## 🔧 自定义修改建议

### 调整难度

- **降低难度**：
  ```
python
  # 第 89-90 行
  base_bullet_count = 2  # 初始弹道改为 2
  base_enemy_spawn = 1   # 保持初始敌机数量为 1
  ```
- **提高挑战性**：
  ```
python
  # 第 127 行 - 缩短射击间隔
  if shoot_timer > 150:  # 原来是 250
  
  # 第 148 行 - 加快敌机生成
  if enemy_timer > 500:  # 原来是 800
  ```
### 修改外观

- **改变颜色**（第 14-18 行）：
  ```
python
  CYAN = (0, 255, 255)    # 玩家飞机颜色 (R, G, B)
  YELLOW = (255, 255, 0)  # 子弹颜色
  RED = (255, 0, 0)       # 敌机颜色
  ```
- **调整窗口大小**（第 8 行）：
  ```
python
  WIDTH, HEIGHT = 800, 1200  # 更大的游戏区域
  ```
### 添加功能

可以扩展的功能：
- 🎵 背景音乐和音效
- 💾 最高分记录系统
- 🎁 道具掉落（生命恢复、火力增强等）
- 👾 Boss 战机制
- 🖼️ 替换为精美的图片素材

## 📝 技术要点

- **碰撞检测**：使用 AABB 算法进行高效的矩形碰撞判断
- **时间控制**：基于 delta time（毫秒）的计时器系统
- **列表管理**：使用切片拷贝 `[:]` 安全地遍历和删除元素
- **递归重启**：游戏结束后通过递归调用 `main()` 实现重新开始

## 🐛 常见问题

### Q: 运行后黑屏或闪退？
A: 检查是否正确安装了 Pygame，尝试重新运行 `pip install pygame`。

### Q: 游戏太卡或太慢？
A: 确保电脑性能足够，关闭其他占用资源的程序。可以尝试降低窗口大小。

### Q: 按键响应不灵敏？
A: 这是正常现象，游戏采用连续按键检测，长按方向键即可持续移动。

## 📄 许可证

本项目为学习用途，基于 Pygame 开发。

## 🙏 致谢

感谢 Pygame 社区提供的优秀工具和文档支持！

---

**祝你游戏愉快！** 🚀
```


